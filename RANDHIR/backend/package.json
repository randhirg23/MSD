{
  "name": "car-rental-backend",
  "version": "1.0.0",
  "description": "Backend for car rental application",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "npm install --include=dev",
    "render:build": "npm run dev && npm --prefix ../frontend install --include=dev && npm --prefix ../frontend run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
